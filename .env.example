# SCA Environment Configuration
# Copy this file to .env and fill in your credentials

# ================================
# GitHub Configuration
# ================================
# For creating GitHub issues from security findings

# GitHub personal access token
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
# Note: If you use 'gh' CLI and are authenticated, this is optional
GITHUB_TOKEN=

# GitHub repository in format: owner/repo
# Example: mycompany/myapp
GITHUB_REPO=

# ================================
# Jira Configuration
# ================================
# For creating Jira tickets from security findings

# Jira instance URL (without trailing slash)
# Example: https://yourcompany.atlassian.net
JIRA_URL=

# Jira username or email
# Example: security-team@company.com
JIRA_USER=

# Jira API token
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=

# Jira project key
# Example: SEC, VULN, SECURITY
JIRA_PROJECT=

# Jira issue type (default: Bug)
# Common types: Bug, Task, Story, Security
JIRA_ISSUE_TYPE=Bug

# ================================
# Optional: Custom Jira Fields
# ================================
# If your Jira instance has custom fields for security findings

# Custom field ID for Finding ID (optional)
# JIRA_CUSTOM_FINDING_ID=customfield_10000

# Custom field ID for Severity (optional)
# JIRA_CUSTOM_SEVERITY=customfield_10001

# ================================
# General Configuration
# ================================

# Control directory path (optional, defaults to auto-detect)
# SEC_CTRL_DIR=./sec-ctrl

# SCA agent home (optional, defaults to auto-detect)
# SEC_AUDIT_AGENT_HOME=/opt/sca

# ================================
# Usage Examples
# ================================
#
# Create GitHub issues (Critical/High only):
#   sca create-tickets --platform github
#
# Create Jira tickets (all severities):
#   sca create-tickets --platform jira --severity-min LOW
#
# Dry run to preview:
#   sca create-tickets --dry-run
#
# ================================
# Security Notes
# ================================
#
# 1. NEVER commit .env file to git
# 2. Keep .env file permissions restricted: chmod 600 .env
# 3. Rotate API tokens regularly (every 90 days recommended)
# 4. Use dedicated service accounts for automation
# 5. Consider using a secrets manager for production deployments
