# sec-ctrl (control directory)

This directory stores repo-specific state for Security Control Agent (SCA).
It is the ONLY place the audit tooling should write artifacts.

Recommended to keep out of VCS or commit only what you intend (e.g., config and local overrides).
